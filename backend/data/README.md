# Data Directory

This directory contains CSV files for:
- users.csv - User credentials
- leads.csv - Leads data
- recruiters.csv - Recruiters data
- applications_{username}.csv - Per-user application data

These files are NOT tracked in git to preserve data privacy and prevent overwriting existing data.
